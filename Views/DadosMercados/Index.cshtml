@model IEnumerable<PrevisaoEcommerce.Models.DadosMercado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeEmpresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnoPrevisao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeEmpresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnoPrevisao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDadosMerc">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDadosMerc">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDadosMerc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
